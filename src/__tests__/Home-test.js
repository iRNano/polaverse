// // import AnimeServices from '../__mock__/AnimeServices';

// jest.mock('../AnimeServices');

// it('works with promises', () => {
//   expect.assertions(1);
//   return AnimeServices.getTopAnime().then((data) => expect(data).toHaveReturned());
// });
