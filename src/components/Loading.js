import classes from './Loading.module.css'
const Loading = () => {
    return(
        <div className={classes.loading}>
            <h1>Loading...</h1>
        </div>
    )
}

export default Loading;